# Copy this file to .env.local and fill in the values from your Supabase
# project settings (Project Settings -> API).

NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-public-key

# Optional: used as a fallback when constructing email confirmation redirect
# links from a context where an Origin header isn't available. Set this to
# your deployed URL in Vercel's environment variables. Also used by the
# proxy (src/proxy.ts) to tell platform traffic apart from community custom
# domains, so keep it pointing at the platform's own URL — never at a
# community's domain.
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Required for email invites (admin -> "Invite by email") and for community
# custom domains (admin -> "Custom domain"). Find it in
# Project Settings -> API -> Project API keys -> service_role.
# NEVER prefix this with NEXT_PUBLIC_ and never commit a real value — it
# bypasses Row Level Security entirely. Only read server-side (see
# src/lib/supabase/admin.ts).
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Optional: powers the AI-synthesized answer on the Concierge page (see
# src/lib/ai/concierge-answer.ts) and staff-facing AI event discovery on the
# Events page (see src/lib/ai/discover-events.ts). Without it, Concierge
# still works — it just shows Layer 1's plain grouped search results with no
# summary — and event discovery reports itself as unavailable.
# Get a key at https://console.anthropic.com/settings/keys. Server-side only.
ANTHROPIC_API_KEY=your-anthropic-api-key

# Optional: enables the "Generate image" button on crop guides and the
# propose-a-crop form (see src/lib/ai/crop-image.ts), which synthesises a crop
# illustration with OpenAI's image model. Without it, that button is hidden;
# the "Find photo with AI" button still works (it uses web search / Wikipedia
# and needs no extra key). Get a key at https://platform.openai.com/api-keys.
# Server-side only.
# OPENAI_API_KEY=sk-xxxxxxxxxxxx

# Optional: lets "Check verification" on the community admin page also
# register the custom domain with the Vercel project automatically (routing
# + SSL), making domain setup fully self-serve — see src/lib/vercel-domains.ts.
# Without these, the platform operator adds each domain by hand in
# Vercel -> Settings -> Domains. Create the token at
# https://vercel.com/account/tokens; the project ID is in the Vercel
# project's Settings -> General. VERCEL_TEAM_ID only if the project lives in
# a team. All server-side only.
# VERCEL_TOKEN=your-vercel-api-token
# VERCEL_PROJECT_ID=prj_xxxxxxxxxxxx
# VERCEL_TEAM_ID=team_xxxxxxxxxxxx

# Optional: sends community-branded invite emails through Resend
# (see src/lib/email.ts) — "You're invited to Mzungu Zanzibar", from the
# community's name, with its logo, and without pre-creating a passwordless
# auth account the way Supabase's inviteUserByEmail does. Without it, email
# invites fall back to Supabase Auth's global "from Relate" template.
# Requires the sending domain to be verified in Resend (resend.com ->
# Domains). INVITE_EMAIL_FROM defaults to invites@<NEXT_PUBLIC_SITE_URL
# hostname>. Both server-side only.
# RESEND_API_KEY=re_xxxxxxxxxxxx
# INVITE_EMAIL_FROM=invites@relate.click
# Sender for notification emails (e.g. a business-directory claim to review, or
# a decision on your claim). Also sent through Resend, so it needs the same
# verified domain; defaults to notifications@<NEXT_PUBLIC_SITE_URL hostname>.
# Without RESEND_API_KEY, the in-app bell notification still fires — only the
# email is skipped. Server-side only.
# NOTIFICATION_EMAIL_FROM=notifications@relate.click
# Where the public /contact form delivers. The recipient is server-side only —
# it never reaches the browser. Defaults to relate@guestlist.net; CONTACT_EMAIL_FROM
# is the sender and defaults to contact@<NEXT_PUBLIC_SITE_URL hostname>, then to
# the invites sender. Submissions are always stored in public.contact_messages,
# so a missing RESEND_API_KEY only skips the email, never the record.
# CONTACT_EMAIL_TO=relate@guestlist.net
# CONTACT_EMAIL_FROM=contact@relate.click

# Optional: emails the in-app notifications (comment, post, membership, claim)
# in addition to the bell. A DB trigger posts each new notification to
# /api/notifications/email, which sends it via Resend. This secret authenticates
# that webhook — it must match the notification_email_webhook_secret row you add
# to the public.app_config table (see
# supabase/migrations/*_email_on_notification_insert.sql for the one-time setup).
# Without it (and RESEND_API_KEY), notifications simply stay in-app only.
# NOTIFICATION_EMAIL_WEBHOOK_SECRET=some-long-random-string

# Optional: browser/mobile push notifications, the push counterpart of the email
# above. A second DB trigger posts each new notification to /api/notifications/push,
# which delivers it via Web Push. Generate a VAPID keypair once with
# `npx web-push generate-vapid-keys` and set the values below. The push webhook
# reuses NOTIFICATION_EMAIL_WEBHOOK_SECRET (and the same app_config row), so no
# second secret is needed. The public key is safe to expose to the browser (it
# needs it to subscribe); the private key is server-only — NEVER prefix it with
# NEXT_PUBLIC_. Without these, push simply stays off (email/bell are unaffected).
# VAPID_PUBLIC_KEY=your-vapid-public-key
# VAPID_PRIVATE_KEY=your-vapid-private-key
# NEXT_PUBLIC_VAPID_PUBLIC_KEY=your-vapid-public-key
# VAPID_SUBJECT=mailto:notifications@relate.click

# Optional: shows each business's live Google rating and review snippets in
# the map pin popup (see src/lib/google-places.ts). Needs "Places API (New)"
# enabled on the key in Google Cloud Console. Without it, popups still work —
# they just skip the Google section. Server-side only.
GOOGLE_PLACES_API_KEY=your-google-places-api-key

# Optional: shows a member's own crops from the shamba.online farm app inside a
# Crop Guides space ("My crops" panel). relate and the farm app are separate
# Supabase projects, so relate does NOT hold the farm database's keys — it calls
# a small read-only endpoint the farm app exposes, matched by the signed-in
# user's email (see src/lib/farm-bridge.ts). Without these, the panel simply
# hides. FARM_API_URL / FARM_API_SECRET are server-side only; the shared secret
# must match the one the farm endpoint checks. NEXT_PUBLIC_FARM_APP_URL is the
# public link used by the "Manage in shamba.online" button. Members can also opt
# their farm public so others in the community can browse their crops — that
# browse path reads the opted-in members' emails with SUPABASE_SERVICE_ROLE_KEY
# (above), so it degrades to empty when the service-role key is unset.
# FARM_API_URL=https://shamba.online/api/external/my-crops
# FARM_API_SECRET=a-long-random-shared-secret
# NEXT_PUBLIC_FARM_APP_URL=https://shamba.online

# Optional: the production video backend for Live Events spaces (JaaS — Jitsi
# as a Service, by 8x8). Without these, a Live Events space still works but
# embeds the FREE PUBLIC meet.jit.si server, which is demo-only: calls drop
# after 5 minutes and show a "use Jitsi as a Service" banner. Set all three to
# switch to authenticated, unlimited rooms. Get them from the JaaS console
# (https://jaas.8x8.vc -> API Keys): JITSI_APP_ID is the AppID/tenant
# ("vpaas-magic-cookie-…"), JITSI_API_KEY_ID is the key id, and
# JITSI_PRIVATE_KEY is the matching RSA private key (PEM). In .env, put the PEM
# on one line with literal "\n" between the lines. All server-side only (see
# src/lib/jitsi.ts).
# JITSI_APP_ID=vpaas-magic-cookie-xxxxxxxxxxxxxxxx
# JITSI_API_KEY_ID=vpaas-magic-cookie-xxxxxxxxxxxxxxxx/xxxxxx
# JITSI_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nMIIE...\n-----END PRIVATE KEY-----"

# Optional: enables per-space paywalls — community owners connect their own
# Stripe account (Connect Express) and charge members a monthly fee for access
# to individual spaces (Admin -> Payments, then set a price on a space). Owners
# are the merchant of record and keep 100% of each charge; the platform takes no
# application fee here. Without STRIPE_SECRET_KEY, the Payments section explains
# that payments aren't enabled and no space can be priced.
#
# STRIPE_SECRET_KEY is the platform account's secret key (sk_test_… / sk_live_…)
# from https://dashboard.stripe.com/apikeys — it must have Connect enabled.
# STRIPE_WEBHOOK_SECRET (whsec_…) authenticates the webhook at
# /api/stripe/webhook: create an endpoint in the Stripe dashboard for the events
# checkout.session.completed, customer.subscription.created/updated/deleted and
# account.updated, then copy its signing secret here. Both server-side only —
# NEVER prefix with NEXT_PUBLIC_ (see src/lib/stripe.ts).
# STRIPE_SECRET_KEY=sk_test_xxxxxxxxxxxx
# STRIPE_WEBHOOK_SECRET=whsec_xxxxxxxxxxxx

